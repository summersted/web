/*
$(window).scroll(function(){
    if ($(window).scrollTop() > 200) {
        $('.header').addClass('scroll');
        $('.scroll').removeClass('header')
    }
    else {
        $('.header').removeClass('scroll')
        $('.scroll').addClass('header');
    }
});